import { TimestampValidationPipe } from './timestamp-validation.pipe';

describe('TimestampValidationPipe', () => {
  it('should be defined', () => {
    expect(new TimestampValidationPipe()).toBeDefined();
  });
});
